# Core API (Phase 0+) — light enough for a work laptop
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.7
pydantic-settings==2.9.1
sqlalchemy==2.0.41
psycopg2-binary==2.9.10
pgvector==0.3.6
python-jose[cryptography]==3.3.0
httpx==0.28.1
python-dotenv==1.1.0
pytest==8.3.5

# ML (Phase 1+) — heavier; install with: pip install -r requirements-ml.txt
# Kept separate so Phase 0 health/DB checks stay fast on Windows i5.
